HOST=0.0.0.0
PORT=8000
DEBUG=True
RELOAD=True

CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

API_V1_PREFIX=/api/v1
API_TITLE=AI Video Retriever API
API_VERSION=1.0.0

DATA_ROOT=app/data
KEYFRAME_DIR=app/data/keyframe
INDEX_DIR=app/data/index
MAPPING_KF_PATH=app/data/index/mapping_kf.json
MAPPING_SCENE_PATH=app/data/index/mapping_scene.json

DEFAULT_TOP_K=200

EMBEDDING_SERVER_MULTIMODAL=
EMBEDDING_SERVER_QWEN = 

GEMINI_API_KEYS=
COHERE_API_KEYS=

ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_INDEX_PREFIX=es_data
ELASTICSEARCH_REQUEST_TIMEOUT=60
ELASTICSEARCH_MAX_RETRIES=3
ELASTICSEARCH_RETRY_ON_TIMEOUT=true
ELASTICSEARCH_WAIT_TIMEOUT=30


QDRANT_HOST=localhost
QDRANT_GRPC_PORT=6334
QDRANT_HTTP_PORT=6333
QDRANT_TIMEOUT=30
QDRANT_WAIT_TIMEOUT=0
QDRANT_RETRY_ATTEMPTS=5
QDRANT_RETRY_DELAY=3
QDRANT_BATCH_SIZE=500

LOG_DIR=logs

# Chatbox settings
CHATBOX_DB_PATH=app/data/chatbox.db
CHATBOX_ENABLED=True
CHATBOX_MAX_LIMIT=200

# PostgreSQL settings
# For local Docker PostgreSQL container:
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_DB=aivideo_chatbox
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres

# For AWS RDS PostgreSQL (Cloud Database):
# Replace with your actual RDS endpoint and credentials
POSTGRES_HOST=aivideo-chatbox-db.cfyiso2w8rqi.ap-southeast-2.rds.amazonaws.com
POSTGRES_PORT=5432
POSTGRES_DB=aivideo_chatbox
POSTGRES_USER=<YOUR_RDS_USERNAME>
# IMPORTANT: If password contains special characters (like ==, #, $, etc.), 
# wrap it in double quotes: POSTGRES_PASSWORD="Nhan2212005=="
# Or use single quotes: POSTGRES_PASSWORD='Nhan2212005=='
POSTGRES_PASSWORD=<YOUR_RDS_PASSWORD>
POSTGRES_POOL_SIZE=5
POSTGRES_MAX_OVERFLOW=10

